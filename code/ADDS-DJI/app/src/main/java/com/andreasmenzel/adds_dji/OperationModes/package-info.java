/**
 * The Operation Mode describes the "low-level" action the drone is currently performing. This can
 * be compared to DJIs Flight Modes (e.g. "GPS", "AUTO_TAKEOFF", ...). The Operation Modes are
 * higher level than the DJI Flight Modes, however.
 */
package com.andreasmenzel.adds_dji.OperationModes;